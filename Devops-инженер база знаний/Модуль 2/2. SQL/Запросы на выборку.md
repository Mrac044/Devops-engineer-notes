**`SELECT`** - c помощью этой команды выполняют выборку данных из таблиц.

## Как сделать выборку данных

#SQL-команда  начала рассмотрим простую его форму:

```
SELECT * FROM users;
```
[View on DB Fiddle](https://www.db-fiddle.com/f/r6dUARm8zSZ7JMMXD3XVzR/0)

Схематично его можно обозначить так:
![[Pasted image 20260126180708.png]]

#SQL-команда Если поля нужны не все, то достаточно перечислить их через запятую вместо звездочки:

```SQL
SELECT username, email FROM users;
```
[View on DB Fiddle](https://www.db-fiddle.com/f/r6dUARm8zSZ7JMMXD3XVzR/1)

На схеме обозначается так:
![[Pasted image 20260126180751.png]]

#SQL-команда Поля необязательно перечислять в том же порядке, в котором они идут в таблице:

```SQL
SELECT first_name, email, username FROM users;
```
[View on DB Fiddle](https://www.db-fiddle.com/f/r6dUARm8zSZ7JMMXD3XVzR/4)

==В результате такого запроса поля таблицы `users` выведутся в том порядке, который указан в части `SELECT`.==

```SQL
FROM users SELECT first_name, email, username;
```

Такой запрос не выполнится, хотя в нем и присутствуют те же операторы. Дело в том, что при составлении запроса очень важен порядок операторов:

1. Сначала указываем оператор SELECT
2. Затем перечисляем столбцы
3. Потом идет оператор FROM
4. Затем мы указываем имя таблицы, откуда выбираем данные

Этот порядок написания запросов называется **синтаксисом** запроса.

Вообще говоря, СУБД не зависит от регистра. Она выполнит запрос, даже если он будет записан так:

```SQL
SelECT FirSt_Name, eMail, USERname FRom Users;
```

[View on DB Fiddle](https://www.db-fiddle.com/f/r6dUARm8zSZ7JMMXD3XVzR/5)

Но читать такой запрос очень сложно: не совсем понятно, где названия полей, а где — таблица, из которой выбираются данные.

## Дополнительные материалы

- [Как практиковаться с выполнением запросов с сервисом DB Fiddle](https://help.hexlet.io/practice-guides/kak-praktikovatsya-v-vypolnenii-sql-zaprosov/)
- [DB Fiddle](https://www.db-fiddle.com/f/sqWurpoChNH82TCba5tyKD/14).