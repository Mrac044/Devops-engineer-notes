[Документация github на анлийском](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/about-ssh)
# Подробная инструкция по настройке SSH доступа к GitHub на Ubuntu

## ==Шаг 1: Проверка наличия существующих SSH ключей

```bash
# Посмотреть есть ли уже SSH ключи
ls -al ~/.ssh
```

Если вы видите файлы типа `id_rsa.pub`, `id_ed25519.pub` или `id_ecdsa.pub`, то у вас уже есть SSH ключи.

## ==Шаг 2: Генерация нового SSH ключа (если нет существующего)

```bash
# Генерация ключа ED25519 (рекомендуется)
ssh-keygen -t ed25519 -C "doctor.06@bk.ru"

# Или, если ваша система не поддерживает ED25519:
ssh-keygen -t rsa -b 4096 -C "doctor.06@bk.ru"
```

**Во время генерации:**
1. Нажмите Enter для принятия пути по умолчанию (`~/.ssh/id_ed25519`)
2. Введите пароль (passphrase) для дополнительной безопасности или оставьте пустым

## ==Шаг 3: Добавление SSH ключа в SSH-агент

```bash
# Запуск SSH-агента в фоновом режиме
eval "$(ssh-agent -s)"
# Должен вывести что-то вроде: Agent pid 12345

# Добавьте приватный ключ в SSH-агент
ssh-add ~/.ssh/id_ed25519
# Или для RSA ключа:
# ssh-add ~/.ssh/id_rsa
```

**Для автоматического запуска SSH-агента при старте системы, добавьте в `~/.bashrc` или `~/.zshrc`:**
```bash
if [ -z "$SSH_AUTH_SOCK" ]; then
   # Check for a currently running instance of the agent
   RUNNING_AGENT="$(ps -ax | grep 'ssh-agent -s' | grep -v grep | wc -l | tr -d '[:space:]')"
   if [ "$RUNNING_AGENT" = "0" ]; then
        # Launch a new instance of the agent
        ssh-agent -s &> $HOME/.ssh/ssh-agent
   fi
   eval $(cat $HOME/.ssh/ssh-agent)
fi
```

## ==Шаг 4: Копирование публичного SSH ключа

```bash
# Скопируйте содержимое публичного ключа в буфер обмена
cat ~/.ssh/id_ed25519.pub | xclip -selection clipboard
# Или, если xclip не установлен:
cat ~/.ssh/id_ed25519.pub
# Затем выделите и скопируйте вывод вручную
```

**Или установите xclip:**
```bash
sudo apt install xclip
```

## ==Шаг 5: Добавление SSH ключа в аккаунт GitHub

1. **Откройте GitHub в браузере**
2. Перейдите в **Settings** (Настройки)
3. В левом меню выберите **SSH and GPG keys**
4. Нажмите **New SSH key** или **Add SSH key**
5. Заполните поля:
   - **Title**: Название (например, "Ubuntu Desktop" или "Work Laptop")
   - **Key type**: Оставьте "Authentication Key"
   - **Key**: Вставьте скопированный публичный ключ

## ==Шаг 6: Проверка соединения

```bash
# Проверьте подключение к GitHub
ssh -T git@github.com
```

Вы должны увидеть:
```
Hi username! You've successfully authenticated, but GitHub does not provide shell access.
```

## Шаг 7: Настройка Git для использования SSH

```bash
# Установите глобальное имя пользователя и email (если еще не сделано)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш_email@example.com"

# Настройте Git для использования SSH по умолчанию
git config --global url."git@github.com:".insteadOf "https://github.com/"
```

## Шаг 8: Работа с репозиториями

**Для клонирования репозитория используйте SSH URL:**
```bash
# Вместо https://github.com/username/repo.git
git clone git@github.com:username/repo.git
```

**Если уже склонировали через HTTPS, можно изменить URL:**
```bash
cd existing-repo
git remote set-url origin git@github.com:username/repo.git
```

## Шаг 9: Конфигурация SSH (опционально)

Создайте/отредактируйте файл `~/.ssh/config` для удобства:

```bash
nano ~/.ssh/config
```

Добавьте:
```ssh
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519
    IdentitiesOnly yes
```

Сохраните (`Ctrl+X`, затем `Y`, затем `Enter`).

## Шаг 10: Устранение проблем

### Если возникает ошибка "Permission denied (publickey)":

1. **Проверьте ключ на GitHub:**
```bash
ssh -T git@github.com -v
```

2. **Убедитесь что ключ добавлен в агент:**
```bash
ssh-add -l
```

3. **Перезапустите SSH-агент:**
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

4. **Проверьте права доступа к файлам:**
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
```

### Для нескольких SSH ключей:

Если у вас несколько аккаунтов GitHub, создайте отдельный ключ:

```bash
ssh-keygen -t ed25519 -C "work_email@company.com" -f ~/.ssh/id_ed25519_work
```

И добавьте в `~/.ssh/config`:
```ssh
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes
```

Используйте с кастомным хостом:
```bash
git clone git@github-work:company/project.git
```

## Полезные команды для проверки:

```bash
# Проверить существующие ключи
ls -la ~/.ssh/

# Показать fingerprint ключа
ssh-keygen -l -f ~/.ssh/id_ed25519.pub

# Показать какие ключи загружены в агент
ssh-add -l

# Удалить все ключи из агента
ssh-add -D
```

## Автоматизация (опционально):

Создайте скрипт для быстрой настройки:
```bash
#!/bin/bash
# setup-github-ssh.sh
echo "Generating SSH key..."
ssh-keygen -t ed25519 -C "$1"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
echo "Your public key:"
cat ~/.ssh/id_ed25519.pub
echo "Copy the above key to GitHub!"
```

Теперь у вас настроен SSH доступ к GitHub! 🎉
